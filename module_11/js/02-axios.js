/*const API_KEY = "38249273yp57r3yp";

const list = document.querySelector(".list");
axios("http://", {
    params: {
        key: API_KEY,
        q: "rose"
    }
})
    .then(res => {
        console.log(res)
        list.insertAdjacentElement("beforeend", createMarkup(res.data.hits))
    })
    .catch(error => {
        console.log(error)
    })
*/